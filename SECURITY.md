# Security

Do not open public issues containing API keys, personal media, prompts with
sensitive information, or signing credentials.

For a suspected vulnerability, contact the repository owner privately through
the email address listed on the GitHub profile for `rijalarogya`.

LoopForge validates model-generated intent against imported files and supported
operations. It constructs a `Process` executable and argument array internally;
it does not execute model-generated shell commands.
